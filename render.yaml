services:
  - type: web
    name: investara
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Agent_Kit.fast_api:app --host 0.0.0.0 --port 10000
    plan: free
